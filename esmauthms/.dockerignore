# Compiled output — Docker builds from source, not from pre-built JARs
target/

# IDE and editor files
.idea/
*.iml
.vscode/
*.code-workspace

# OS noise
.DS_Store
Thumbs.db

# Git metadata
.git/
.gitignore

# Docker files (no need to copy them into the image)
docker/
docker-compose*.yml
*.md